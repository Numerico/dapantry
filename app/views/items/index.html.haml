%ul
  - @items.each do |item|
    %li
      %span= item.name
      %span= check_box_tag nil, nil, item.hay
      %span= link_to 'Destroy', item, :method => :delete, :data => { :confirm => 'Are you sure?' }
%br

= render 'form'
